"use client";
import Loader from "@/src/components/common/loader";
import React from "react";

const loading = () => {
  return (
    <div>
      Loading...
      <Loader />
    </div>
  );
};

export default loading;
